{
  "name": "vite-pug-scss-project",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm-run-all --parallel vite sass pug",
    "vite": "vite",
    "sass": "sass assets/scss:assets/css --no-source-map --watch",
    "pug": "pug assets/pug/pages/template --pretty --out template --watch",
    "build": "vite build"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "pug-cli": "^1.0.0-alpha6",
    "sass": "1.77.6",
    "vite": "^4.0.0"
  }
}
